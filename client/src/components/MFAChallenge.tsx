/**
 * MFA Challenge Screen (Stub)
 *
 * MFA is handled by the Manus OAuth provider, so this component
 * is a no-op placeholder.
 */
export default function MFAChallenge({ onVerified, onCancel }: { onVerified?: () => void; onCancel?: () => void }) {
  return null;
}
