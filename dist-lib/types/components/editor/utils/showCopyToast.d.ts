/**
 * Show a brief toast notification for copy actions.
 * Creates a temporary DOM element that auto-removes after 1.5s.
 * Adapts styling to dark/light mode.
 */
export declare function showCopyToast(message: string): void;
